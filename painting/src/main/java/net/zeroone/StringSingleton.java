package net.zeroone;


public class StringSingleton {
    /**
     * a static variable is shared across the entire application
     * 
     */
    public static String data;

    /**
     * Let's say that we need to distribute the exact same string across our entire app.
     * But, we only need to set the string value once! we don't want to re-set it.
     * 
     * If this were a DB Connection, we don't want to kill the connection when the connection is 
     * 
     * 
     */
}